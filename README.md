# AI Daily Digest

从 Karpathy 推荐的 90 个热门技术博客（Hacker News Popularity Contest 2025）抓取最新文章，通过 DeepSeek AI 评分/摘要，生成结构化的 Markdown 每日精选。

## 功能

- 批量抓取 RSS/Atom，按时间范围过滤
- AI 评分（相关性/质量/时效性）+ 自动分类（AI/安全/工程/工具/观点/其他）
- 生成中文标题、结构化摘要、推荐理由、关键词
- 生成可读的 Markdown 报告：Top 3、分类列表、Mermaid 图表、话题标签

## 项目结构

```
.
├── README.md              # 本文件
├── daily-digest.sh        # 一键运行脚本（推荐）
└── .opencode/
    └── skills/
        └── ai-daily-digest/
            ├── SKILL.md   # OpenCode Skill 文档
            └── scripts/
                └── digest.ts  # 主程序（RSS 抓取 + AI 评分）
```

## 运行环境

- Node.js（用于 `npx`）
- `bun`（可通过 `npx -y bun` 自动拉起）
- DeepSeek API Key（通过环境变量 `DEEPSEEK_API_KEY` 提供）

## 快速开始（推荐：一键脚本）

最简单的方式是使用 `daily-digest.sh` 一键运行脚本：

### 首次使用

需要通过 OpenCode 配置：

```bash
opencode
```

在 OpenCode 中输入 `/digest`，完成配置后会自动保存到 `~/.hn-daily-digest/config.json`

### 后续使用

配置完成后，直接运行：

```bash
./daily-digest.sh
```

脚本会自动：
- 读取 `~/.hn-daily-digest/config.json` 配置
- 生成报告到 `./output/` 目录
- 在终端高亮显示结果

### 全局使用（可选）

如果你想在任意目录下直接执行脚本，可以将脚本复制到 home 目录并设置环境变量：

```bash
# 1. 复制脚本到 home 目录
cp daily-digest.sh ~/

# 2. 在 ~/.zshrc 或 ~/.bashrc 中添加环境变量
export AI_DAILY_DIGEST_HOME="/Users/mori/codes/ai-daily"

# 3. 重新加载配置
source ~/.zshrc

# 4. 之后可以在任意位置执行
~/daily-digest.sh
```

**环境变量说明：**

| 变量名 | 说明 | 必需 |
|--------|------|------|
| `AI_DAILY_DIGEST_HOME` | 项目根目录路径（包含 `.opencode/` 的目录） | 否，仅当脚本不在项目目录时需要 |
| `DEEPSEEK_API_KEY` | DeepSeek API Key | 是（手动运行时需要） |

## 快速开始（手动运行）

如果你不想使用一键脚本，也可以直接运行：

1. 设置环境变量：

```bash
export DEEPSEEK_API_KEY="<your-deepseek-api-key>"
```

2. 运行（默认：48 小时、15 篇、中文）：

```bash
mkdir -p ./output

npx -y bun ./.opencode/skills/ai-daily-digest/scripts/digest.ts \
  --hours 48 \
  --top-n 15 \
  --lang zh \
  --output ./output/digest-$(date +%Y%m%d).md
```

### 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--hours <n>` | 抓取最近 n 小时的文章 | 48 |
| `--top-n <n>` | 最终保留 n 篇精选 | 15 |
| `--lang <zh\|en>` | 输出语言 | zh |
| `--output <path>` | 输出文件路径 | ./digest-YYYYMMDD.md |

## 终端显示优化

脚本会自动检测并使用以下工具美化 Markdown 显示：

| 优先级 | 工具 | 说明 |
|--------|------|------|
| 优先 | `glow` | 专门的终端 Markdown 渲染器（推荐） |
| 备选 | `bat` | 支持语法高亮、行号、Git 差异 |
| 回退 | `cat` | 普通文本显示 |

安装推荐工具：

```bash
# macOS
brew install glow

# 或安装 bat
brew install bat
```

## OpenCode Skill 用法

本仓库包含一个 OpenCode skill：`ai-daily-digest`。

- 入口文档：[`.opencode/skills/ai-daily-digest/SKILL.md`](.opencode/skills/ai-daily-digest/SKILL.md)
- 主脚本：[`.opencode/skills/ai-daily-digest/scripts/digest.ts`](.opencode/skills/ai-daily-digest/scripts/digest.ts)

如果你在 OpenCode 里加载了该 skill，可使用 `/digest` 走交互式流程（时间范围、精选数量、输出语言、API Key）。

## 输出内容

生成的 Markdown 报告包含：

- `## 📝 今日看点`：3-5 句趋势总结
- `## 🏆 今日必读`：Top 3 深度展示（标题/摘要/理由/标签）
- `## 📊 数据概览`：统计表 + Mermaid 分类饼图/关键词柱状图 + 话题标签云
- 分类文章列表：按 6 大分类分组（AI / 安全 / 工程 / 工具 / 观点 / 其他）

## 配置说明

配置文件路径：`~/.hn-daily-digest/config.json`

```json
{
  "deepseekApiKey": "your-api-key",
  "timeRange": 48,
  "topN": 15,
  "language": "zh",
  "lastUsed": "2026-02-14T12:00:00Z"
}
```

## API Key 安全

- 项目代码不会把 Key 写入仓库：脚本只读取 `process.env.DEEPSEEK_API_KEY`
- 不要把 Key 直接写进源码或提交到 GitHub
- 配置文件保存在用户主目录下（`~/.hn-daily-digest/`），不会被提交到仓库

## 技术栈

- **运行时**: Bun (TypeScript)
- **AI 模型**: DeepSeek Chat
- **数据源**: 90 个 RSS/Atom 订阅源
- **输出**: Markdown + Mermaid 图表
